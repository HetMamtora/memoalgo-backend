spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/memoalgo_dev
    username: postgres
    password: personalProjects1324@
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2

  jpa:
    show-sql: true

jwt:
  secret: dev-secret-key-replace-this-in-production-with-256-bit-key
  expiration-ms: 86400000
  refresh-expiration-ms: 604800000

# Resend, called via its HTTPS API now (not SMTP) -- see EmailService.
# Reuse the same API key you already have; nothing about the key itself
# changed, only how EmailService uses it.
app:
  resend:
    api-key: re_U876uMFX_McMzJRoZxUp31vxgLZd558PR
  mail:
    from: "MemoAlgo <onboarding@resend.dev>"
  cors:
    allowed-origins: http://localhost:5173

logging:
  level:
    com.memoalgo: DEBUG
    org.springframework.security: DEBUG
    org.flywaydb: DEBUG
